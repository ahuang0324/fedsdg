# Copyright (c) JLU, NODI, Huimin Huang. All rights reserved.
# Licensed under the Apache License, Version 2.0 (the "License");
# WandB 日志后端
# @package _global_
#
# 使用方式:
#   python main.py logger=wandb
#   python main.py logger=wandb logging.wandb_mode=offline  # 离线模式
#
logging:
  backend: wandb
  
  # Project name grouped by dataset.
  wandb_project: "fl-${dataset.name}"
  
  # Group runs from the same dataset partition setting.
  wandb_group: "${dataset.name}_alpha${federated.dirichlet_alpha}"
  
  # Run name summarizing algorithm and key hyperparameters.
  wandb_run_name: "${algorithm.name}_E${training.epochs}_lr${training.lr}_seed${system.seed}_${now:%m%d_%H%M}"
  
  wandb_job_type: "train"
  wandb_tags:
    - "${algorithm.name}"
    - "${model.name}"
    - "seed=${system.seed}"
    - "alpha=${federated.dirichlet_alpha}"
  
  wandb_mode: offline
  log_every_n_rounds: 1
  save_config: true
